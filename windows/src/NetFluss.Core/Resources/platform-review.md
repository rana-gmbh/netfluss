# Platform vocabulary review

Generated by `windows/tools/strings2resx.py`. Every string below mentions a
platform-specific concept that may not be correct on Windows. Rows marked
*auto-rewritten* were changed by `PLATFORM_OVERRIDES`; rows marked *needs review*
were left alone and want a human decision.

| Language | Key | Status | Value |
| --- | --- | --- | --- |
| `en` | `System Default follows the language selected in macOS.` | auto-rewritten | System Default follows the language selected in Windows. |
| `en` | `When enabled, the Download/Upload summary and menu bar use only adapters that are visible here.` | needs review | When enabled, the Download/Upload summary and menu bar use only adapters that are visible here. |
| `en` | `Menu bar icon style` | needs review | Menu bar icon style |
| `en` | `Menu bar icon` | needs review | Menu bar icon |
| `en` | `Menu bar size` | needs review | Menu bar size |
| `en` | `Menu bar font` | needs review | Menu bar font |
| `en` | `Menu bar unit` | needs review | Menu bar unit |
| `en` | `DNS changes and Ethernet reconnects install a privileged helper the first time. macOS may ask for administrator approval and, on some systems, additional approval in System Settings.` | auto-rewritten | DNS changes and Ethernet reconnects install a privileged helper the first time. Windows may ask for administrator approval and, on some systems, additional approval in Settings. |
| `en` | `Location access is required to list nearby Wi-Fi networks. macOS uses the same restriction in System Settings → Wi-Fi.` | auto-rewritten | Location access is required to list nearby Wi-Fi networks. Windows uses the same restriction in Settings → Wi-Fi. |
| `en` | `Denied — open System Settings to allow` | auto-rewritten | Denied — open Settings to allow |
| `en` | `Required to list nearby Wi-Fi networks. macOS uses the same gate for its own Wi-Fi menu.` | auto-rewritten | Required to list nearby Wi-Fi networks. Windows uses the same gate for its own Wi-Fi menu. |
| `en` | `Used to change DNS servers and to persist new Wi-Fi credentials into macOS's Known Networks. macOS may show a "Background Items Added" approval prompt the first time.` | auto-rewritten | Used to change DNS servers and to persist new Wi-Fi credentials into Windows's Known Networks. Windows may show a "Background Items Added" approval prompt the first time. |
| `en` | `No system VPNs found. Configure one in System Settings → VPN, or install a provider configuration profile below.` | auto-rewritten | No system VPNs found. Configure one in Settings → VPN, or install a provider configuration profile below. |
| `en` | `Creates an IKEv2 profile NetFluss connects directly (username/password). The password is stored in your Keychain. If the server uses a private CA, trust its certificate in Keychain Access first.` | auto-rewritten | Creates an IKEv2 profile NetFluss connects directly (username/password). The password is stored in your Credential Manager. If the server uses a private CA, trust its certificate in Credential Manager Access first. |
| `en` | `Reveal VPN log in Finder` | needs review | Reveal VPN log in Finder |
| `en` | `When enabled, VPN/tunnel adapters (utun, tun, tap, ipsec, ppp) are excluded from totals. Loopback and AirDrop are always excluded, since they never carry internet traffic. All adapters remain visible in the adapter list.` | needs review | When enabled, VPN/tunnel adapters (utun, tun, tap, ipsec, ppp) are excluded from totals. Loopback and AirDrop are always excluded, since they never carry internet traffic. All adapters remain visible in the adapter list. |
| `en` | `Who your Mac is talking to right now.` | needs review | Who your Mac is talking to right now. |
| `de` | `System Default follows the language selected in macOS.` | auto-rewritten | Systemsprache folgt der in Windows ausgewählten Sprache. |
| `de` | `DNS changes and Ethernet reconnects install a privileged helper the first time. macOS may ask for administrator approval and, on some systems, additional approval in System Settings.` | auto-rewritten | DNS-Änderungen und Ethernet-Neuverbindungen installieren beim ersten Mal ein privilegiertes Hilfsprogramm. Windows kann nach Administratorfreigabe und auf manchen Systemen nach zusätzlicher Freigabe in den Systemeinstellungen fragen. |
| `de` | `Recent results saved on this Mac. Add notes to remember the exact place.` | needs review | Aktuelle Ergebnisse, die auf diesem Mac gespeichert sind. Füge Notizen hinzu, um den genauen Ort festzuhalten. |
| `de` | `Location access is required to list nearby Wi-Fi networks. macOS uses the same restriction in System Settings → Wi-Fi.` | auto-rewritten | Standortzugriff ist erforderlich, um nahegelegene WLAN-Netzwerke aufzulisten. Windows verwendet die gleiche Einschränkung unter Systemeinstellungen → WLAN. |
| `de` | `Required to list nearby Wi-Fi networks. macOS uses the same gate for its own Wi-Fi menu.` | auto-rewritten | Erforderlich, um nahegelegene WLAN-Netzwerke aufzulisten. Windows verwendet dieselbe Berechtigungsschwelle für sein eigenes WLAN-Menü. |
| `de` | `Used to change DNS servers and to persist new Wi-Fi credentials into macOS's Known Networks. macOS may show a "Background Items Added" approval prompt the first time.` | auto-rewritten | Wird verwendet, um DNS-Server zu ändern und neue WLAN-Anmeldedaten in Windows' bekannten Netzwerken zu speichern. Beim ersten Mal zeigt Windows möglicherweise eine "Hintergrundobjekte hinzugefügt"-Genehmigungsaufforderung. |
| `de` | `NetFluss will record per-interface byte counters for %.0f seconds. After you click Start, begin a large download (for example a Speed Test) and let it run until the capture finishes. The diagnostics will then be copied to your clipboard so you can paste them into a GitHub issue.

No network traffic is sent — only local counters from your machine are recorded.` | needs review | NetFluss zeichnet die Byte-Zähler pro Schnittstelle für %.0f Sekunden auf. Nachdem Sie auf „Starten" geklickt haben, starten Sie einen großen Download (zum Beispiel einen Speedtest) und lassen Sie ihn bis zum Ende der Aufzeichnung laufen. Die Diagnose wird anschließend in Ihre Zwischenablage kopiert, damit Sie sie in ein GitHub-Issue einfügen können.

Es wird kein Netzwerkverkehr gesendet — nur lokale Zähler von Ihrem Mac werden aufgezeichnet. |
| `de` | `%d snapshots were captured. Paste them into the GitHub issue (issue #31) so the developer can identify which interface accumulates download bytes on your Mac.` | needs review | %d Aufnahmen wurden erfasst. Fügen Sie sie in das GitHub-Issue (#31) ein, damit der Entwickler erkennen kann, welche Schnittstelle auf Ihrem Mac Download-Bytes ansammelt. |
| `de` | `Reveal VPN log in Finder` | needs review | VPN-Protokoll im Finder zeigen |
| `de` | `When enabled, VPN/tunnel adapters (utun, tun, tap, ipsec, ppp) are excluded from totals. Loopback and AirDrop are always excluded, since they never carry internet traffic. All adapters remain visible in the adapter list.` | needs review | Wenn aktiviert, werden VPN-/Tunnel-Adapter (utun, tun, tap, ipsec, ppp) aus den Summen ausgeschlossen. Loopback und AirDrop werden immer ausgeschlossen, da sie keinen Internetverkehr führen. Alle Adapter bleiben in der Adapterliste sichtbar. |
| `de` | `Who your Mac is talking to right now.` | needs review | Mit wem dein Mac gerade spricht. |
| `zh-Hans` | `System Default follows the language selected in macOS.` | auto-rewritten | 系统默认会跟随 Windows 中选择的语言。 |
| `zh-Hans` | `DNS changes and Ethernet reconnects install a privileged helper the first time. macOS may ask for administrator approval and, on some systems, additional approval in System Settings.` | auto-rewritten | 首次更改 DNS 或重新连接以太网时会安装特权辅助工具。Windows 可能会要求管理员批准，在某些系统上还需要在系统设置中额外批准。 |
| `zh-Hans` | `Recent results saved on this Mac. Add notes to remember the exact place.` | needs review | 保存在这台 Mac 上的最近结果。添加备注以记住具体地点。 |
| `zh-Hans` | `Location access is required to list nearby Wi-Fi networks. macOS uses the same restriction in System Settings → Wi-Fi.` | auto-rewritten | 需要位置权限才能列出附近的 Wi-Fi 网络。Windows 在「系统设置 → Wi-Fi」中使用相同的限制。 |
| `zh-Hans` | `Required to list nearby Wi-Fi networks. macOS uses the same gate for its own Wi-Fi menu.` | auto-rewritten | 列出附近 Wi-Fi 网络所必需的权限。Windows 在其自身的 Wi-Fi 菜单中使用相同的权限关卡。 |
| `zh-Hans` | `Used to change DNS servers and to persist new Wi-Fi credentials into macOS's Known Networks. macOS may show a "Background Items Added" approval prompt the first time.` | auto-rewritten | 用于更改 DNS 服务器并将新的 Wi-Fi 凭据保存到 Windows 的已知网络中。首次使用时，Windows 可能会显示「已添加后台项目」的批准提示。 |
| `zh-Hans` | `%d snapshots were captured. Paste them into the GitHub issue (issue #31) so the developer can identify which interface accumulates download bytes on your Mac.` | needs review | 已捕获 %d 个快照。请将其粘贴到 GitHub Issue (#31)，以便开发者识别您 Mac 上哪个接口在累积下载字节。 |
| `zh-Hans` | `When enabled, VPN/tunnel adapters (utun, tun, tap, ipsec, ppp) are excluded from totals. Loopback and AirDrop are always excluded, since they never carry internet traffic. All adapters remain visible in the adapter list.` | needs review | 启用后，VPN/隧道适配器（utun、tun、tap、ipsec、ppp）将从总计中排除。回环和 AirDrop 始终被排除，因为它们不承载互联网流量。所有适配器仍会显示在适配器列表中。 |
| `zh-Hans` | `Who your Mac is talking to right now.` | needs review | 你的 Mac 此刻正在与谁通信。 |
| `zh-Hant` | `System Default follows the language selected in macOS.` | auto-rewritten | 系統預設會跟隨 Windows 中選擇的語言。 |
| `zh-Hant` | `DNS changes and Ethernet reconnects install a privileged helper the first time. macOS may ask for administrator approval and, on some systems, additional approval in System Settings.` | auto-rewritten | 首次變更 DNS 或重新連接乙太網路時會安裝特權輔助工具。Windows 可能會要求管理員批准，在某些系統上還需要在系統設定中額外批准。 |
| `zh-Hant` | `Recent results saved on this Mac. Add notes to remember the exact place.` | needs review | 保存在這台 Mac 上的最近結果。加入備註以記住具體地點。 |
| `zh-Hant` | `Location access is required to list nearby Wi-Fi networks. macOS uses the same restriction in System Settings → Wi-Fi.` | auto-rewritten | 需要位置權限才能列出附近的 Wi-Fi 網路。Windows 在「系統設定 → Wi-Fi」中使用相同的限制。 |
| `zh-Hant` | `Required to list nearby Wi-Fi networks. macOS uses the same gate for its own Wi-Fi menu.` | auto-rewritten | 列出附近 Wi-Fi 網路所需的權限。Windows 在自家 Wi-Fi 選單中使用相同的權限關卡。 |
| `zh-Hant` | `Used to change DNS servers and to persist new Wi-Fi credentials into macOS's Known Networks. macOS may show a "Background Items Added" approval prompt the first time.` | auto-rewritten | 用於變更 DNS 伺服器並將新的 Wi-Fi 認證儲存至 Windows 的已知網路。首次使用時，Windows 可能會顯示「已加入背景項目」的批准提示。 |
| `zh-Hant` | `%d snapshots were captured. Paste them into the GitHub issue (issue #31) so the developer can identify which interface accumulates download bytes on your Mac.` | needs review | 已擷取 %d 個快照。請將其貼到 GitHub Issue (#31)，以便開發者判斷您 Mac 上哪個介面在累積下載位元組。 |
| `zh-Hant` | `Reveal VPN log in Finder` | needs review | 在 Finder 中顯示 VPN 日誌 |
| `zh-Hant` | `When enabled, VPN/tunnel adapters (utun, tun, tap, ipsec, ppp) are excluded from totals. Loopback and AirDrop are always excluded, since they never carry internet traffic. All adapters remain visible in the adapter list.` | needs review | 啟用後，VPN/通道介面（utun、tun、tap、ipsec、ppp）將從總計中排除。回送與 AirDrop 一律排除，因為它們不承載網際網路流量。所有介面仍會顯示在介面清單中。 |
| `zh-Hant` | `Who your Mac is talking to right now.` | needs review | 你的 Mac 此刻正在與誰通訊。 |
